# CLI Commands

See the canonical command reference at [`commands.md`](../../../../commands.md) in the repository root.

This CLI exposes all commands documented there. Run `screenmcp --help` or type `help` in shell mode.
